type Namespace = {
    id: number;
    name: string;

    globalDomain: string;
    
    serverHost: string;
    serverPort: number;
    serverUsername: string;
    serverPrivateKey: string;
}

export default Namespace