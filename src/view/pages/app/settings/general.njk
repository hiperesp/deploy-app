<div class="col-md-6 pt-3">
    <form>
        <input type="hidden" name="redirect" value="{{ originalUrl }}">

        <h4 class="mb-2">General Settings</h4>
        <p class="mb-4 text-body-secondary">General settings for your app.</p>

        <div class="form-group mb-3">
            <label class="fw-bold">App name</label>
            <div class="input-group">
                <input type="text" class="form-control" value="{{ app.name }}" disabled>
                <button type="submit" class="btn btn-dark" disabled>Rename</button>
            </div>
        </div>

    </form>
</div>
